__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store

# Your best.pt isn't needed in production (only best.onnx is used by app.py) -
# keep it out of git since it's redundant weight, not because it's too big.
models/*.pt

